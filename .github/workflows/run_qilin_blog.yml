name: Run Qilin Blog

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.x  # Specify the Python version you need

    - name: Install dependencies
      run: pip install -r requirements.txt

    - name: Download and install Firefox and Geckodriver
      run: |
        # Install Firefox
        sudo apt-get update
        sudo apt-get install firefox -y

        # Install Geckodriver
        GECKODRIVER_VERSION="your_geckodriver_version"  # Replace with the Geckodriver version you need
        wget https://github.com/mozilla/geckodriver/releases/download/${GECKODRIVER_VERSION}/geckodriver-${GECKODRIVER_VERSION}-linux64.tar.gz
        tar -xvzf geckodriver-${GECKODRIVER_VERSION}-linux64.tar.gz
        chmod +x geckodriver
        sudo mv geckodriver /usr/local/bin/

    - name: Run scraper
      run: python Qilin_Blog.py  # Replace with the actual filename of your script
